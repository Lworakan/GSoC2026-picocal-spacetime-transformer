# Stage 1: single-photon, uniform 40 mm cells, no background.
experiment: stage1_cellular_automaton
data:
  stage: 1
  path: data/full/     # git-ignored; matched_*.root from the mentors
  tree: clusters_matched
  knn_k: 8
model:
  name: cellular_automaton
evaluation:
  metrics: [bias, rmse, p68]
