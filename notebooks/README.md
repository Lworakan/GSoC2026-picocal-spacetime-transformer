# Notebooks

- `01_data_exploration.ipynb` — shower profiles, timing distributions, occupancy,
  feature correlations (Milestone 1, >=6 diagnostic plots).

Keep notebooks lightweight: clear outputs before committing, and move reusable
code into `src/picocal/` so it can be tested.
