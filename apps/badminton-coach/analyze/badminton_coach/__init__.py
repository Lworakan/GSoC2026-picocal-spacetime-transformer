"""Badminton posture and stroke analysis.

The package splits into a thin MediaPipe layer (:mod:`extract`) and pure-maths
modules that work on plain lists of numbers, so the analysis can be unit-tested
and kept in step with the browser app that ships alongside it.
"""

__version__ = "0.1.0"
