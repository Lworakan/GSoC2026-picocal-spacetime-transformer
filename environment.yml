name: picocal
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pyyaml
  - pip
  - pip:
      - torch
      - torch-geometric
      - uproot
      - awkward
      - plotly
      - bokeh>=3.9,<4
      - ipywidgets>=8.1.8
      - ipykernel
      - pytest
      - ruff
