"""Cellular Automaton baseline (simplest LHCb clustering algorithm).

Rule-based, no ML, no timing. Used to establish the end-to-end pipeline
(Milestone 1).
"""
from __future__ import annotations


def reconstruct(graph):
    """Predict cluster energy with a cellular-automaton rule.

    TODO(Phase 1): seed + neighbour-merge rule, return predicted energies.
    """
    raise NotImplementedError("Implement Cellular Automaton baseline in Phase 1.")
